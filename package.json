{
  "name": "leetcode-solutions-vault",
  "private": true,
  "scripts": {
    "dev": "npm --prefix website run dev",
    "build": "npm --prefix website run build",
    "preview": "npm --prefix website run preview",
    "deploy": "npm --prefix website run build && firebase deploy --only hosting"
  }
}
